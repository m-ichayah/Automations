# assets

Place the official Dangote logo here as `dangote-logo.png` (or `.jpg`), then run:

    node deck/build-deck.js

The title slide picks it up automatically and renders it into a 2.5" x 1.05"
box at the top-left. If no file is present, a marked placeholder slot of the
same size and position is drawn instead.
